<Window x:Class="AmentumRevit.Views.ProjectParameterBrowserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Amentum - Panel View"
        Width="1200" Height="720"
        MinWidth="900" MinHeight="520"
        WindowStartupLocation="CenterScreen">
    <DockPanel>
        <Border DockPanel.Dock="Top" Background="#003865" Padding="16,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Amentum - Panel View" Foreground="White" FontSize="18" FontWeight="SemiBold"/>
                <TextBlock x:Name="CountLabel" Foreground="#A0B8CC" FontSize="13" VerticalAlignment="Bottom" Margin="16,0,0,3"/>
            </StackPanel>
        </Border>
        <Border DockPanel.Dock="Top" Background="#F5F7FA" BorderBrush="#D0D8E0" BorderThickness="0,0,0,1" Padding="10">
            <DockPanel>
                <TextBlock Text="Filter:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                <TextBox x:Name="FilterBox" Width="340" TextChanged="FilterBox_TextChanged"/>
                <TextBlock x:Name="StatusLabel" VerticalAlignment="Center" Foreground="#555" Margin="14,0,0,0"/>
            </DockPanel>
        </Border>
        <Border DockPanel.Dock="Bottom" Background="#F5F7FA" BorderBrush="#D0D8E0" BorderThickness="0,1,0,0" Padding="10">
            <DockPanel>
                <Button Content="Close" DockPanel.Dock="Right" Padding="16,6" Click="Close_Click"/>
                <TextBlock Text="Select elements before opening Panel View to use edit/apply mode." VerticalAlignment="Center" Foreground="#555"/>
            </DockPanel>
        </Border>
        <DataGrid x:Name="MainGrid"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  EnableRowVirtualization="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Preview" Width="74">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Width="54" Height="54" Background="#EEF2F6" BorderBrush="#D0D8E0" BorderThickness="1" Margin="4">
                                <Image Source="{Binding Preview}" Stretch="Uniform"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="160"/>
                <DataGridTextColumn Header="Family" Binding="{Binding Family}" Width="180"/>
                <DataGridTextColumn Header="Type" Binding="{Binding TypeName}" Width="180"/>
                <DataGridTextColumn Header="Parameter" Binding="{Binding ParameterName}" Width="210"/>
                <DataGridTextColumn Header="Scope" Binding="{Binding Scope}" Width="120"/>
                <DataGridTextColumn Header="Storage / Data Type" Binding="{Binding StorageType}" Width="220"/>
                <DataGridTextColumn Header="Shared" Binding="{Binding IsShared}" Width="70"/>
                <DataGridTextColumn Header="Path" Binding="{Binding Path}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>
